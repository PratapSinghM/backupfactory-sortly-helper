VITE_API_BASE=

# If set to your Cloudflare Worker URL, requests will go through the proxy
# and the app will send the raw cookie in an 'x-proxy-cookie' header which
# the worker forwards to Sortly as 'Cookie'.
# Example:
# VITE_API_BASE=https://sortly-proxy.yourname.workers.dev

